############################################################
# Global Environment Template (sanitized)
#
# Notes:
# - Do NOT put real API keys in this repository.
# - Place provider keys in ~/.config/artemis/env (make artemis-setup).
# - For service-specific config in this repo, prefer:
#     - .env.sophia (infrastructure only) → see .env.sophia.example
#     - .env.mcp (MCP runtime) → see .env.mcp.example
############################################################

# Example infrastructure (local defaults)
REDIS_URL=redis://localhost:6379
WEAVIATE_URL=http://localhost:8080

# HTTP defaults
REQUEST_TIMEOUT=30
MAX_RETRIES=2
LOG_LEVEL=INFO

# Provider routing (example toggles only)
USE_PORTKEY_ROUTING=false
ENABLE_FALLBACK=true
ENABLE_LOAD_BALANCING=false

# Provider keys are intentionally omitted here.
# Run: make artemis-setup, then set keys in ~/.config/artemis/env
