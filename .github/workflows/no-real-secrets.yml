name: no-real-secrets

on:
  pull_request:
  push:
    branches: [ main ]

jobs:
  scan:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Run lightweight secret scan
        run: bash scripts/check_no_real_secrets.sh

