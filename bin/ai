#!/usr/bin/env bash
# Deprecated unified AI router (in-repo)
# Policy: Provider CLIs must be external-only. Do not route or invoke them from this repo.
echo "[FORBIDDEN] 'bin/ai' is disabled. Use external Toolbox CLIs (installed to ~/.local/bin)." >&2
echo "See docs/EXTERNAL_TOOLS.md for install and usage." >&2
exit 2
