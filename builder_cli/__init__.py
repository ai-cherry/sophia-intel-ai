"""
builder_cli - Canonical Builder CLI package for Sophia Intel AI

This package provides the importable modules used by the bridge API and
the root CLI entrypoint. It replaces ad-hoc imports of the legacy
`builder-cli` directory (hyphen), which is not importable as a Python package.
"""

