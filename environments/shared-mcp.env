# Shared MCP Server Configuration
# Used by both sophia-intel-ai backend and workbench-ui frontend

# MCP Server URLs
MCP_MEMORY_URL=http://localhost:8081
MCP_FILESYSTEM_URL=http://localhost:8082
MCP_GIT_URL=http://localhost:8084
MCP_VECTOR_URL=http://localhost:8085

# MCP Server Ports (for reference)
MCP_MEMORY_PORT=8081
MCP_FILESYSTEM_PORT=8082
MCP_GIT_PORT=8084
MCP_VECTOR_PORT=8085

# Authentication (Development Mode)
MCP_TOKEN=dev-token
MCP_DEV_BYPASS=1

# Backend Services
REDIS_URL=redis://localhost:6379
# Weaviate Cloud Configuration
WEAVIATE_URL=https://w6bigpoxsrwvq7wlgmmdva.c0.us-west3.gcp.weaviate.cloud
WEAVIATE_API_KEY=VMKjGMQUnXQIDiFOciZZOhr7amBfCHMh7hNf
WEAVIATE_GRPC_ENDPOINT=grpc-w6bigpoxsrwvq7wlgmmdva.c0.us-west3.gcp.weaviate.cloud
WEAVIATE_REST_ENDPOINT=w6bigpoxsrwvq7wlgmmdva.c0.us-west3.gcp.weaviate.cloud

# Workspace Configuration
WORKSPACE_PATH=/Users/lynnmusil/sophia-intel-ai
WORKBENCH_UI_PATH=/Users/lynnmusil/workbench-ui

# Python Path
PYTHON_BIN=python3

# Logging
LOG_LEVEL=info
LOG_DIR=logs

# Development Settings
DEV_MODE=true
DEBUG=false

# Timeout Settings (milliseconds)
MCP_REQUEST_TIMEOUT=30000
MCP_HEALTH_CHECK_INTERVAL=30000

# Retry Configuration
MCP_MAX_RETRIES=3
MCP_RETRY_DELAY=1000
MCP_BACKOFF_MULTIPLIER=2

# Feature Flags
VECTOR_ENABLED=true
VECTOR_SKIP_IF_UNAVAILABLE=true
TIME_BOUNDED_INDEXING=true
TIME_BUDGET_SECONDS=10

# Production overrides (uncomment for production)
# MCP_DEV_BYPASS=0
# MCP_TOKEN=<your-production-token>
# DEV_MODE=false
# DEBUG=false