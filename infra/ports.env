# Mapping reference for MCP ports
# canonical uses MCP_MEMORY_PORT=8081, MCP_FILESYSTEM_PORT=8082, MCP_GIT_PORT=8084, MCP_VECTOR_PORT=8085
# docker-compose uses PORT_MCP_MEMORY=..., etc.
# Prefer MCP*_PORT; map to compose vars as needed.

# Canonical
MCP_MEMORY_PORT=8081
MCP_FILESYSTEM_PORT=8082
MCP_GIT_PORT=8084
MCP_VECTOR_PORT=8085

# Compose-style (mapped from canonical if needed)
PORT_MCP_MEMORY=${MCP_MEMORY_PORT}
PORT_MCP_FILESYSTEM=${MCP_FILESYSTEM_PORT}
PORT_MCP_GIT=${MCP_GIT_PORT}
PORT_MCP_VECTOR=${MCP_VECTOR_PORT}

