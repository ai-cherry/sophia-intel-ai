apiVersion: v1
kind: Namespace
metadata:
  name: monitoring
  labels:
    name: monitoring
    monitoring: enabled
    prometheus: enabled
    alertmanager: enabled
    istio-injection: enabled
  annotations:
    description: "AlertManager and monitoring stack namespace"
